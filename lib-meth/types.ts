/** @format */

export type Hz = number; // Hertz (1/second)
export type ms = number; // Milliseconds
export type vw = number; // viewport width
export type vh = number; // viewport height
export type viewUnits = number; // Viewport Units (vw or vh)
export type viewPerSec = number; // Viewport Units (vw or vh) per Second

export type percent = number; // 0 to 0.9999999...

export type timeoutID = number; // An event ID returned by window.setTimeout()
export type intervalID = number; // An event ID returned by window.setInterval()

export type htmlAttributeName = string;
export type htmlAttributeValue = string;
export type cssPropertyName = string;
export type cssPropertyValue = string;

export type char = string;
export type key = string;
