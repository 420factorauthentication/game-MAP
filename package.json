{
    "dependencies": {
        "spriteling": "^1.1.5",
        "uuid": "^9.0.0"
    },
    "devDependencies": {
        "@types/jest": "^29.2.0",
        "jest": "^29.2.2",
        "ts-jest": "^29.0.3"
    },
    "scripts": {
        "test": "jest",
        "preinstall": "npm install -g typescript && npm install -g prettier"
    }
}
